# локальные конфиги Claude Code / превью-сервера
.claude/
# мусор macOS
.DS_Store
# исходники картинок/шрифтов — не деплоятся, лежат локально
uploads/
# локальный инструмент генерации QR (содержит доступ к служебному ключу) — не деплоим
qr.html
